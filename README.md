# A Shell in C

It is currently able to handle output redirects, run commands, run a script with comments, use a shebang









# Temp, remove later
I am handling file redirection and the command data structure is an array of strings. I have an if statement in main where the parent (pid > 0) is waiting and the child currently runs the file redirection check and then executes with execvp.
